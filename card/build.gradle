dependencies {
  compile project(':')
}

jar {
  manifest {
    attributes 'Main-Class': 'org.fic.card.FicCardCLI',
      'Class-Path': configurations.compile.collect { '../../../libs/' + it.getName() }.join(' ')
  }
}